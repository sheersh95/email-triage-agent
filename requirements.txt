# Gmail API
google-api-python-client>=2.120.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# LLM (for later days)
anthropic>=0.40.0

# Agent framework (for later days)
langgraph>=0.2.0
langchain-anthropic>=0.2.0

# Config
python-dotenv>=1.0.0
pydantic>=2.6.0

# UI (for later days)
streamlit>=1.32.0
